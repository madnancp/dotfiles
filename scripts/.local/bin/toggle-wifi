#!/usr/bin/env bash

if [ "$(nmcli radio wifi)" = "enabled" ]; then
	nmcli radio wifi off
	notify-send "󰖪 Wifi" "Wifi Disabled" --urgency critical
else
	nmcli radio wifi on
	notify-send " Wifi" "Wifi Enabled"
fi
